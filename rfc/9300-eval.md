# 9300 — Eval

Check that the harness behaves correctly. **Status: implemented** in `src/evals.nim` — 34 offline checks run by `nimo eval` (no model/GPU needed).

Covered today:
- Tool-call parsing (all 3 forms)
- Loop termination (max-iteration guard, aborted turns)
- Session JSONL shape (header + message chain with parent ids)
- State-cache hit/miss
- Chapter validation logic
- FIAAS search ranking
- Branch save/load round-trip

Below are the original model-behavior examples (kept for reference):

## Example: tool calling

```nim
# Input
System: You have tool get_weather(location)
User: What's the weather in Boston?

# Expected output contains:
<tool_call>{"get_weather": {"location": "Boston, MA"}}</tool_call>

# Pass: output has correct tool name + args
```

## Example: think blocks

```nim
# Input
User: Why is the sky blue?

# Expected: assistant thinks before answering
Assistant: 
The sky appears blue because...
```

## Metrics

- Tool call accuracy: correct tool + args
- Think presence: `` parsed
- Multi-turn consistency: 3+ turns, no context loss
