# 6000 — Source Structure

Desired layout for the project.

```
lib/
  rwkv.so           # Base engine
  rwkv_cuda.so      # CUDA backend
  rwkv_vulkan.so    # Vulkan backend
  nimo.so           # Shared Nim library

app/
  cli.nim           # CLI entry point
  server.nim        # Network server
  client.nim        # Network client
  session.nim       # Session management
  rwkv.nim          # FFI bridge
  model.nim         # Model abstraction
  tokenizer.nim     # Tokenization
  protocol/
    user_intent.nim   # Intent → pipeline
    just_chatting.nim # Chat with tools
    workspace_related.nim # Workspace ops
    story_related.nim   # Story generation
```

## Current vs Desired

| Current | Desired |
|---------|---------|
| `src/` (flat) | `lib/`, `app/`, `protocol/` |
| `rwkv.nim` | `lib/rwkv.so` + `app/rwkv.nim` |
| `session.nim` (raw inference) | `app/session.nim` (message model) |

## Module map (current src/ -> RFC)

| Module | Role | RFC |
|--------|------|-----|
| `rwkv.nim` | FFI wrapper + eval overloads | 8100 |
| `tokenizer.nim` | vocab encode/decode | 8100 |
| `sampling.nim` | logits sampling (temp/topP) | 8100 |
| `session.nim` | low-level RWKV session | 1000 |
| `session_manager.nim` | messages, tools, JSONL | 1000, 1100 |
| `harness.nim` | agent loop, tool parsing, CLI | 3400, 1100, 2000 |
| `harness_main.nim` | CLI entry point | 2000 |
| `pipeline.nim` | `run_pipeline` tool | 3000 |
| `gpu.nim` | GPU probe, fallback, VRAM clamp | 7500 |
| `model_cache.nim` | raw->quantize->cache | 8150 |
| `state_cache.nim` | context->state->cache | 8000 |
| `config.nim` | `NimoConfig` + env | 4000 |
| `cli.nim` | shared CLI styling/errors | 2000 |
| `main.nim`, `chat.nim`, `bake_state.nim`, `generate.nim`, `nimwave_app.nim` | app commands | 2000, 1200, 8000 |
| `logger.nim`, `macros.nim` | logging + `checkOk`/`timeBlock` helpers | 9100 |
| `evals.nim` | offline eval suite (37 checks) | 9300 |
| `test_rwkv_full.nim`, `test_tokenizer.nim` | unit tests | 9400 |

## See Also

- [7000-env.md](7000-env.md) — build outputs
- [1000-session.md](1000-session.md) — session model
