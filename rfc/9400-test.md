# Test

## Test Categories

- **Unit** — individual functions (tokenizer, sampling)
- **Integration** — full pipeline (model load → generate)
- **E2E** — CLI commands, workspace ops
- **Performance** — benchmark suites

## Test Structure

```
tests/
├── unit/
│   ├── test_tokenizer.nim
│   └── test_sampling.nim
├── integration/
│   └── test_pipeline.nim
├── e2e/
│   └── test_cli.nim
└── performance/
    └── test_benchmarks.nim
```

## Current Implementation

`src/test_rwkv_full.nim` — integration test.
`src/test_tokenizer.nim` — tokenizer round-trip test.

## RFC Scope

- Test runner (Nimble task)
- Test data fixtures
- CI integration
- Test coverage reporting
